{
  "name": "tabula-extension",
  "version": "1.0.0",
  "description": "Tabula – a customizable new tab extension. Your space, no noise.",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "node scripts/build.mjs",
    "preview": "vite preview",
    "clean": "node scripts/clean.mjs",
    "test": "vitest --run",
    "test:watch": "vitest",
    "test:ui": "vitest --ui",
    "test:coverage": "vitest --run --coverage",
    "type-check": "tsc --noEmit",
    "lint:firefox": "web-ext lint -s dist",
    "pack:chrome": "cd dist && zip -r ../tabula-chrome.zip .",
    "pack:firefox": "cd dist && zip -r ../tabula-firefox.zip ."
  },
  "author": "Saatvik Sharma",
  "license": "MIT",
  "devDependencies": {
    "@types/chrome": "^0.0.266",
    "@types/node": "^24.7.2",
    "@vitest/ui": "^3.2.4",
    "jsdom": "^27.0.0",
    "typescript": "^5.9.2",
    "vite": "^7.1.7",
    "vitest": "^3.2.4",
    "web-ext": "^7.11.0"
  },
  "dependencies": {}
}
